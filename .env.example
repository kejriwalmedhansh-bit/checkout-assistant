SEARCHAPI_KEY=your_key_here
WHATSAPP_PHONE_NUMBER_ID=
WHATSAPP_ACCESS_TOKEN=
WHATSAPP_VERIFY_TOKEN=dealo_webhook_2026
WHATSAPP_FLOW_ID=
ANTHROPIC_API_KEY=your_key_here
# This backend's own public URL (no trailing slash) — used to build /go
# affiliate redirect links. Render URL in prod, ngrok URL for local WhatsApp testing.
PUBLIC_BASE_URL=http://localhost:8000
# Unlocks the /admin/conversations WhatsApp transcript viewer. Leave empty
# to keep it disabled (returns 404).
ADMIN_PASSWORD=
# --- Tracking (see tracking-plan.json) ---
# Team phones (E.164, no "+", comma-separated) tagged is_internal_tester in Mixpanel.
INTERNAL_PHONES=
# Purchase sync (scripts/sync_affiliate_purchases.py; set as GitHub Actions secrets too).
CUELINKS_API_KEY=
INRDEALS_API_TOKEN=
INRDEALS_USERNAME=
MIXPANEL_SERVICE_ACCOUNT_USERNAME=
MIXPANEL_SERVICE_ACCOUNT_SECRET=
