# Runtime dependencies for the Dealo backend.
# Mirrors [project.dependencies] in pyproject.toml. Offline scrape-script deps
# (beautifulsoup4/lxml/requests) live in the pyproject [scripts] extra only.
fastapi>=0.137.2
uvicorn[standard]>=0.49.0
gunicorn>=21.2.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0
httpx>=0.27.0
