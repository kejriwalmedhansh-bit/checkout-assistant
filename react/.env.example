# Base URL of the Dealo FastAPI backend service
VITE_API_BASE_URL=http://localhost:8000

# Mixpanel needs no env var — the project token is a public identifier and lives
# in src/config.js as MIXPANEL_TOKEN.
